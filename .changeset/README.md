# Changesets

Changesets describe user-facing changes to `jotai-orpc`. Run
`npm run changeset`, select the appropriate semantic version bump, and commit
the generated Markdown file with the implementation.

Documentation, test-only, and CI-only changes do not require a changeset.

See the [Changesets documentation](https://github.com/changesets/changesets)
for more information.
